import React from "react"
import Layout from '../components/layout'

const NotFoundPage = () => {
  return(
    <Layout>
      <p>No hay pagina </p>
    </Layout>
    
  )
}

export default NotFoundPage
